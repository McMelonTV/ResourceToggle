package net.neoforged.api.distmarker;

/**
 * Compile-time stub of the NeoForge {@code Dist} enum. Only the constants
 * used by this mod are declared; the real enum is resolved at runtime.
 */
public enum Dist {
    CLIENT,
    DEDICATED_SERVER
}
